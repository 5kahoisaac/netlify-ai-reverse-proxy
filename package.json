{
  "name": "ai-proxy",
  "version": "1.0.0",
  "description": "Reverse proxy for AI model APIs to bypass region restrictions",
  "main": "netlify/functions/proxy.js",
  "scripts": {
    "build": "echo 'No build step required'",
    "dev": "netlify dev"
  },
  "dependencies": {},
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}
